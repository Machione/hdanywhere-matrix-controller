export var config = {
    "defaultMatrixIpAddress": "192.168.1.210", // This should be static and set in the router.
    "staticOutputBays": [7], // List of zero-indexed bays to always avoid switching.
}